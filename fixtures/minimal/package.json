{
  "name": "fixture-minimal",
  "private": true,
  "packageManager": "bun@1.3.13",
  "workspaces": ["packages/*"],
  "devDependencies": {
    "turbo": "2.5.5"
  },
  "scripts": {
    "check": "echo check-ok",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build"
  }
}
